server:
  port: 8080

spring:
  application:
    name: model-unit-tests

management:
  endpoints:
    enabled-by-default: false
    web:
      exposure:
        include: '*'
  metrics:
    export:
      prometheus:
        enabled: true
  endpoint:
    metrics:
      enabled: true
    prometheus:
      enabled: true
    health:
      enabled: true
      show-details: always

